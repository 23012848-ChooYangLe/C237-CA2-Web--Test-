<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Fitness Tracker App</title>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container">
        <br>
        <div class="text-center"><h2>Member Details</h2></div>
        <br>
        <% if (members) { %> <!-- Check if member data exists -->
            <div>
                <p><b>Member Id:</b><br> <%= members.memberId %></p>
                <p><b>Name:</b><br> <%= members.memberName %></p>
                <p><b>Contact:</b><br> <%= members.contact %></p> 
                <p><b>Email:</b><br> <%= members.email %></p>
                <p><b>Image:</b><br><img src="<%= members.image %>" width="45%" alt="<%= members.memberName %> profile pic"></p>
            </div>
        <% } else { %>
            <p>No member found.</p>
        <% } %>
        <a href="/memberPage"><button>Go back to following</button></a>
    </div>
</body>
</html>