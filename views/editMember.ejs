<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
        <title>Fitness Tracker App</title>
    </head>
<body>
    

    <div class="container">
        <br>
        <div class="text-center"><h2>Update Member</h2></div>
        <br>
        <div class="text-center">
          <% if (member) { %> 
            <form action="/members/<%= member.memberId %>/edit" method="POST" enctype="multipart/form-data">
                <label for="name">Member Name:</label><br>
                <input type="text" id="name" name="name" value = <%= member.memberName %> required><br><br>

                <label for="contact">Contact:</label><br>
                <input type="number" id="contact" name="contact" min="0" step="1" value = <%= member.contact %> required><br><br>

                <label for="email">Email:</label><br>
                <input type="email" id="email" name="email" min="0" step="0.01" value = <%= member.email %> required><br><br>

                <label for="image">Current Profile Image:</label><br>
                <input type="text" name="currentImage" value=<%= member.image %> disabled><br>
                <img src = "/<%= member.image %>" width="20%"><br><br>

                New Image:<br> <input type="file" id="image" name="image" accept="image/*" value=<%= member.image %>>
                <br><br>

                <button type="submit">Update Member</button>
            </form>
          <% } else { %>
              <p>No product found.</p>
          <% } %>
          <a href ="/memberPage">Back</a>
        </div>
    </div>
</body>
</html>
