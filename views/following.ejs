<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>Fitness Tracker App</title>
</head>
<body>
    <%- include ('partials/navbar.ejs') %>
    <div class="container mt-3">
        <h2>You're following:</h2>          
        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Userame</th>
                    <th></th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                <% for (let i=0; i<members.length; i++) { %>
                    <tr>
                        <td><a href="/members/<%= members[i].id %>"><%= members[i].memberName %></a></td>
                        <td><a href="/members/<%= members[i].id %>/viewWorkout"><button>View</button></a></td>
                        <td><a href="/members/<%= members[i].id %>/delete"><button>Unfollow</button></a></td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>